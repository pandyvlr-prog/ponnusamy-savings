const fs = require('fs'); let c = fs.readFileSync('index.html', 'utf8'); const m = c.match(/<div id="screen-pnl"[\s\S]*?<\/table>\s*<\/div>\s*<\/div>/); if(m){ let b = m[0]; c = c.replace(b, ''); b = b.replace('<div id="screen-pnl" class="screen" style="display: none;">', '<section id="screen-pnl" class="app-screen">'); b = b.replace(/<header class="app-header">/, '<header class="screen-header">'); b = b.replace(/<!-- Global App P&L Metrics -->/, '<div class="screen-content">\n        <!-- Global App P&L Metrics -->'); b = b.replace(/<\/div>$/, '</div>\n        </div>\n    </section>'); c = c.replace('<!-- SCREEN: NOTEPAD -->', b + '\n\n            <!-- SCREEN: NOTEPAD -->'); fs.writeFileSync('index.html', c); console.log('Fixed'); } else { console.log('Not found'); }
